<Window x:Class="KylidarAddin.BufferInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clip LiDAR to AOI" Height="250" Width="440"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True" Background="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Buffer distance (feet):" Foreground="Black" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="BufferFeetBox" Text="0" Style="{x:Null}" Foreground="Black" Background="White" VerticalContentAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="LiDAR phase(s):" Foreground="Black" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <CheckBox x:Name="Phase1Box" Content="Phase 1" Style="{x:Null}" Foreground="Black" Margin="0,0,14,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="Phase2Box" Content="Phase 2" Style="{x:Null}" Foreground="Black" Margin="0,0,14,0" VerticalAlignment="Center"/>
                <CheckBox x:Name="Phase3Box" Content="Phase 3" Style="{x:Null}" Foreground="Black" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Output .las file:" Foreground="Black" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="OutputPathBox" Style="{x:Null}" Foreground="Black" Background="White" VerticalContentAlignment="Center" Margin="0,0,6,0"/>
            <Button Grid.Column="2" Content="Browse..." Style="{x:Null}" Foreground="Black" Background="White" Padding="10,3" Click="Browse_Click"/>
        </Grid>

        <TextBlock Grid.Row="3" x:Name="ValidationText" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkBtn" Content="Run" Style="{x:Null}" Foreground="Black" Background="White" Padding="14,3" Margin="0,0,8,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancel" Style="{x:Null}" Foreground="Black" Background="White" Padding="14,3" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
